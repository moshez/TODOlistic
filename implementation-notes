* We can just have 6 Maildirs: Incoming/, TODO/, Notification/, Discussion/, Content/ and Spam/
** Incoming is never displayed
* We use getmail to grab mail and deliver it to Incoming/
* We implement "classify(Current Classification, Mail-name, Correct classification)
** That (a) moves (b) adds evidence
* We use RESTful interfaces:
** GET / --> returns json: {"children": ["/TODO/", ...]}
** GET/<child>/ returns json: {"children": "/<child>/archived/", "/<child>/incoming/"
** GET/<child>/<type> returns json: {"children": [ {'url': '/<base>/mail-id, 'headers': {'from, to, subject}

